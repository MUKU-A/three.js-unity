# UNITY_UI_RESEARCH.md — Unityエディタ UI/UX リサーチ（フェーズ0成果物）

> 目的: 「Unityユーザーが一切の違和感なくThree.jsを使える」Webエディタを作るための、
> Unityエディタ(2021.2以降のダークテーマ)の観察に基づく寸法・配色・操作体系の記録。
> **権利ガード**: Unityのスクリーンショット・アイコン・ロゴは一切取り込まない。
> ここに記録するのは観察から得た規則性(数値・比率・配色傾向)のみであり、実装は独自に行う。
> アイコンは Lucide (ISCライセンス) で等価代替する。フォントは Unity 2021.2+ が採用する
> **Inter**(SIL OFL、オープンライセンス)を @fontsource/inter で自前バンドルする。

## 出典
- Unity Manual "Built-in Unity variables (USS)" — エディタ全色が変数表で公開されている
  (docs.unity3d.com/Manual/UIE-USS-UnityVariables.html)。検索スニペットで
  `--unity-colors-window-background: dark #383838 / light #C8C8C8` を確認。
- Unity Editor Foundations (foundations.unity.com) — 「各色は light/dark の変数で管理」
  「Editor は Inter を全プラットフォームの主要フォントとして使用」を確認。
- Unity Learn "Explore the Unity Editor" — デフォルトレイアウト
  (左Hierarchy / 中央Scene+Game / 右Inspector / 下Project+Console、全ウィンドウがタブ化・ドッキング可能)。
- Unity Manual "Unity Hotkeys" / Shortcuts Manager — Q/W/E/R/T/Y、F、Ctrl+D 等。
- UnityCsReference (公開C#リファレンス) — Handles.xAxisColor 等のギズモ軸色。
- 上記が403で直接取得できなかった値は、コミュニティで広く引用される値
  (フォーラム/ギスト/テーマ再現プロジェクト)と実スクリーンショット観察の合意値を採用し、(※)を付す。

---

## 1. 全体レイアウト(デフォルト "Default" レイアウト)

```
┌──────────────────────────────────────────────────────────────┐
│ メインツールバー (高さ ~30px): 左=Move等ツール群 中央=▶⏸⏭ 右=Layers/Layout │
├───────────┬──────────────────────────────┬───────────────────┤
│ Hierarchy │  Scene / Game (タブ)          │ Inspector         │
│ (~17%)    │  (~58%)                       │ (~25%)            │
│           │  ┌ Sceneビュー内ツールバー ┐   │                   │
│           ├──────────────────────────────┤                   │
│           │ Project / Console (タブ)      │                   │
│           │ (下段, 高さ~30%)              │                   │
└───────────┴──────────────────────────────┴───────────────────┘
```

- 既定配置: **左=Hierarchy、中央上=Scene(Gameとタブ共有)、中央下=Project+Console(タブ共有)、右=Inspector**。
- すべてのウィンドウはタブをドラッグして分離・再ドッキング・リサイズ可能。
- パネル間の境界線は1pxの暗い線。ドラッグでリサイズ。
- 比率の目安(1920px幅): Hierarchy ~320px、Inspector ~460px、残りが中央。下段は全高の30%前後。
- 各パネルはタブ(タイトル)を持つ。アクティブタブはパネル背景と同色で"つながって"見える。

## 2. 配色 — Unityダークテーマ近似パレット(独自再現用)

「Unityに近い独自ダークテーマ」として以下を採用する。
(✔=USS変数表・検索で確認済み / ※=コミュニティ合意値・観察近似)

### ベース
| 用途 | 値 | 備考 |
|---|---|---|
| ウィンドウ/パネル背景 | `#383838` | ✔ --unity-colors-window-background |
| より暗い背景(ツリー/リスト領域, alternating) | `#303030` / `#2E2E2E` | ※ Hierarchy/Project の行領域 |
| 最暗背景(入力欄) | `#2A2A2A` | ※ --unity-colors-input_field-background |
| 入力欄ボーダー | `#212121` | ※ |
| オブジェクトフィールド背景 | `#282828` | ※ |
| タブバー(非アクティブ領域) | `#282828` | ※ アクティブタブは #383838 で地続き |
| タブ(非アクティブ) | `#2D2D2D`, hover `#303030` | ※ |
| パネル境界/ウィンドウボーダー | `#242424` | ※ --unity-colors-window-border |
| 汎用ボーダー | `#232323` | ※ --unity-colors-default-border |
| ヘッダ(Inspectorコンポーネントタイトルバー) | `#3E3E3E`, hover `#474747` | ※ inspector_titlebar |
| ツールバー(パネル内) | `#3C3C3C` 下線 `#232323` | ※ |

### テキスト
| 用途 | 値 |
|---|---|
| 標準テキスト | `#D2D2D2` ※ |
| ラベル(Inspector左列) | `#C4C4C4` ※ |
| 淡色/サブ | `#909090` ※ |
| 無効 | `#5D5D5D` ※ |
| エラー | `#FF6B6B` / 警告 `#F4BC02` / リンク `#4C7EFF` ※ |

### インタラクション
| 用途 | 値 |
|---|---|
| 選択ハイライト(フォーカス時) | `#2C5D87` ※ 「Unityの選択青」としてフォーラムで広く引用 |
| 選択ハイライト(非フォーカス時) | `#4D4D4D` ※ |
| 行hover | `rgba(255,255,255,0.055)` ※ |
| ボタン | `#585858`, hover `#676767`, pressed `#46607C`, border `#303030` ※ |
| チェック済みトグル/アクセント | `#3C7EBF` ※ (Unityのアクセント青系) |
| フィールドフォーカスリング | `#3A79BB` 1px ※ |

### Scene/ビューポート
| 用途 | 値 |
|---|---|
| Sceneビュー背景(スカイボックス無効時) | `#292929`〜`#2B2B2B` 単色 ※ |
| グリッド線 | `rgba(255,255,255,0.08)`相当のグレー、10本ごとに濃い線 ※ |
| 選択アウトライン | オレンジ `#FF6600`(子は青) ※ Unity Manual「デフォルトはオレンジ」✔ |

### ギズモ軸色 (UnityCsReference Handles 既定値 ✔)
| 軸 | float RGB | HEX |
|---|---|---|
| X | (0.8588, 0.2431, 0.1137) | `#DB3E1D` (赤) |
| Y | (0.6039, 0.9529, 0.2824) | `#9AF348` (黄緑) |
| Z | (0.2275, 0.4784, 0.9725) | `#3A7AF8` (青) |
| 選択中の軸 | (0.968, 0.951, 0.196) | `#F7F332` (黄) |
| 中央/フリー | (0.8,0.8,0.8,0.93) | `#CCCCCC` |

※ three.jsのTransformControls既定色(純RGB)はUnityと違和感があるため、上記Unity系色に差し替える。

### 再生モード
- 再生中はエディタUI全体に暗いティント(既定では中央ツールバー周辺が着色)。
- 再現方針: 再生中はツールバーとパネル背景にわずかな青みティント `#405060` α~0.25 を重ね、
  ▶ボタンをアクセント色で点灯状態にする。※(既定のPlaymode tintは設定可能な色)

## 3. タイポグラフィ
- フォント: **Inter** (Unity 2021.2+の公式採用 ✔)。実装は @fontsource/inter をバンドル。
  フォールバック: `Inter, "Segoe UI", system-ui, sans-serif`。
- 基本サイズ: **12px** (エディタ標準)。小サイズ 11px (タブ、ステータス)。※
- ウェイト: 標準 400、パネルタブ/コンポーネント名 600(Semi-Bold寄り)。
- 行間: 詰まり気味。ツリー行は行高で制御(下記)。アンチエイリアスは標準。

## 4. Hierarchy パネル
- 行高: **16px** (非常に密) ※。当実装ではクリック性を考慮し 18px を採用(違和感のない範囲)。
- インデント: **14px/階層** ※。展開三角(▶/▼)は行頭、幅~12px。
- 各行: [展開三角][型アイコン][名前]。アイコンは~14px。
- 選択行: フォーカス時 #2C5D87 で行全体をハイライト、テキスト白。非フォーカス #4D4D4D。
- hover行: ごく薄い白オーバーレイ。
- 無効化オブジェクト: 名前・アイコンが淡色(α~0.5)。トグルは行hoverで出るチェックではなく
  Inspector側チェックボックスが本家だが、行左端hoverの目玉/チェックは Scene可視性トグル。
  → 実装: 行右端hoverに目玉アイコン(Scene可視性=無効トグル相当)+Inspector名横チェック。
- 最上段に検索バー+「+」(Create)ボタンのツールバー。
- 右クリック: Create Empty / 3D Object > (Cube, Sphere, Capsule, Cylinder, Plane, Quad) /
  Light > (Directional, Point, Spot) / Camera / Copy / Paste / Duplicate / Rename / Delete。
- ドラッグ&ドロップで親子変更・並べ替え(挿入線表示)。シーン名のルート行が最上部に太字で出る。
- F2でリネーム、ダブルクリックでもリネーム(本家はワンクリック遅延 or F2)。

## 5. Inspector パネル
- 構造: 最上部にオブジェクトヘッダ(アイコン+有効チェック+名前フィールド+Static等)、
  その下にコンポーネントが縦積み。最後に「Add Component」ボタン(中央寄せ、幅~230px)。
- コンポーネントヘッダ: 高さ~22px、背景 #3E3E3E、[折りたたみ三角][アイコン][名前(太字)]
  [有効チェック][⋮メニュー]。1px下線 #232323。クリックで折りたたみ。
- ラベル幅: パネル幅の**~40-42%**(既定labelWidth≈狭パネルで120px前後)※。右側がフィールド。
- 数値フィールド: 高さ~18-19px、背景 #2A2A2A、1px境界 #212121、角丸2px。
- **ラベル横ドラッグ(スクラブ)**: ラベル上でカーソルが左右矢印(ew-resize)になり、
  ドラッグで値が増減する。感度はドラッグ距離×フィールド値スケール。Shiftで粗く、Altで細かく(※感度倍率は独自校正)。
- Transform: Position/Rotation/Scale の3行、各行に X/Y/Z の3フィールド。
  X/Y/Zのミニラベルはフィールド左に置かれ、これもスクラブ可能。Rotationはオイラー角(度)。
- カラーフィールド: 横長スウォッチ(下部に白帯=アルファバー)。クリックでピッカー。
- スライダー: Metallic/Smoothness等は[スライダー+数値]の組。
- チェックボックス: ~14px角、チェック時アクセント青にチェックマーク。
- Add Component: クリックで検索付きドロップダウンメニュー。

## 6. Scene ビュー(Viewport)
- パネル上部に薄いツールバー: 描画モード、2D、ライト、ギズモ表示トグル等(右寄りにGizmosメニュー)。
- グリッド: 1m間隔のグレー線、原点軸線は強調(X=赤系/Z=青系)。遠方フェード。
- ギズモ: 選択オブジェクトの原点に表示。W=Move(3軸矢印+平面ハンドル)、E=Rotate(3円+外周円)、
  R=Scale(3軸末端キューブ+中央キューブ)。軸色は上記表。ホバー/ドラッグ中の軸は黄色。
- 右上にシーンギズモ(方位キューブ)※今回はスコープ外(拡張余地として記録のみ)。
- クリックで選択(レイキャスト)。クリック空振りで選択解除。Ctrl/Cmd+クリックで追加選択。
- カメラ操作: Alt+左ドラッグ=オービット、中ボタン=パン、ホイール=ズーム、右ドラッグ=フライスルー(WASD)。
  → OrbitControlsの既定(左=回転/右=パン)はUnityと概ね互換。左ドラッグ素振りは「選択」なので、
    実装では 左クリック=選択、Alt+左 or 左ドラッグ=オービット(OrbitControls)、右=パン継続を採用。
- F=選択オブジェクトへフォーカス(カメラが対象を包むよう移動)。

## 7. メインツールバー(最上段, 高さ~30px)
- 左: Transformツール群(手のひら/Move/Rotate/Scale/Rect/Transform)…アイコンボタン列、
  その右にピボット/座標系トグル(Center↔Pivot, Global↔Local)。
- **中央: ▶(Play) ⏸(Pause) ⏭(Step)** の3ボタン。再生中は▶が点灯(アクセント色)。
- 右: Undo履歴等は無し(メニュー)。Layers、Layoutドロップダウン。
- 実装: 左=W/E/Rに対応するMove/Rotate/Scaleボタン+Local/Globalトグル、中央=▶⏸⏹、
  右=Save/Load(JSON)とレイアウトリセット。

## 8. アイコン体系(規則性の観察)
- 用途: パネルタブ、Hierarchyの型(Cube等はメッシュアイコン、Light=電球、Camera=カメラ)、
  Inspectorコンポーネントヘッダ、Projectのアセットサムネイル、ツールバーのツール。
- 傾向: 単色ラインアイコン(テキスト色と同輝度)、選択/点灯時のみ色付き。サイズ14-16px。
- **代替**: Lucideアイコン(ISC)で等価置換 — Box/Circle/Sun/Lightbulb/Camera/Video/Image/
  Move3D/Rotate3D/Scale3D/Play/Pause/Square/Search/Plus/Eye/EyeOff/File/Folder等。

## 9. ショートカット表(実装対象)
| キー | 動作 |
|---|---|
| Q | ハンドツール(ビュー) ※実装ではパン切替相当・省略可 |
| **W** | Moveツール |
| **E** | Rotateツール |
| **R** | Scaleツール |
| **F** | 選択オブジェクトにフォーカス |
| **Ctrl/Cmd+D** | 複製 |
| **Delete / Cmd+Backspace** | 削除 |
| **Ctrl/Cmd+Z** | Undo |
| **Ctrl+Y / Cmd+Shift+Z** | Redo |
| Ctrl/Cmd+S | シーン保存(JSONダウンロード) |
| F2 | リネーム(Hierarchy) |
| Ctrl/Cmd+C / V | コピー/ペースト |
| Esc | リネーム/メニューのキャンセル、選択解除はしない |
| Ctrl(ドラッグ中) | 移動/回転/スケールのスナップ(移動0.25→実装では1.0/0.25検討、回転15°、スケール0.1) |
| X/Y/Z入力欄でEnter | 確定、Tab=次フィールド |
| ▶再生中 | 全編集はスナップショットに載らず停止時に復元 |

- スナップ既定値(Unity): Move 0.25(グリッドスナップは1)、Rotate 15°、Scale 0.1。※実装はCtrl押下中に適用。

## 10. 手触りメモ(違和感ゼロのための細部)
- 数値フィールドはドラッグ中にテキスト選択が発生しない(user-select無効+pointer capture)。
- スクラブ感度: 1pxあたり値×0.01+絶対最小0.03/px 程度が本家に近い(観察校正)。位置系は0.01/px固定+Shift×4。
- Inspectorの数値はドラッグ中もリアルタイムでSceneに反映、Undoはドラッグ終了時に1コマンド。
- ギズモドラッグも同様に「ドラッグ開始→終了」で1 Undo。
- Hierarchyの選択はマウスダウン時(アップではない)。複数選択: Ctrl+クリック=トグル、Shift+クリック=範囲。
- 再生(▶)を押すとGameビューに切替わるのが本家だが、本実装はScene=Gameを兼ねるViewportのため
  ツールバー点灯+ティントで状態を明示する(DECISIONS.md参照)。
- Console: 情報/警告/エラーのフィルタトグル+Clearボタンが上部ツールバーにある。行はアイコン+一行要約。
- Project: 下部にサイズスライダのあるグリッド/リスト表示。今回は簡易リスト+サムネイル。

## 11. three.js editor から借りる設計(UIはUnity、同期思想はthree.js editor)
- 「Signal(イベント)経由で全パネルが疎結合に同期する」— 本実装ではZustandの購読が同役割。
- 「オブジェクトはUUIDで管理、シリアライズはJSON」— 本実装もID正規化JSON。
- 「AddObjectCommand等のCommand群でUndo/Redo」— 本実装もCommandパターン全通し。
